package com.hydra.pica.plataforma_pica.user.domain;

/** Agrupa los permisos para la pantalla de roles. Coincide con {@code Modulo} del contrato. */
public enum Modulo {
    USUARIOS,
    PERSONAS,
    ROLES
}
