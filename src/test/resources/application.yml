spring:
  application:
    name: plataforma-pica
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false

app:
  cors:
    allowed-origins:
      - http://localhost:3000
      - http://localhost:5173
  admin:
    username: admin
    email: admin@pica.local
    initial-password: test-initial-password
